## Как запустить серверы апи и сайта

Шорткат для одновременного запуска:
```shell
grunt run-servers
# NOTE: все флаги запуска доступны, т.е.
grunt run-servers --no-token
# будет работать также как если бы запускалось через `node`
```
