mixin holder(holder)
    a(href='/user/' + holder.id).holder=holder.name

mixin tag(tag)
    a.label(class=('label-' + tag.theme), href='/tag/' + tag.name)=tag.name

mixin headerProfile()
    - if (me) {
       a(href="/user/" + me.id)= me.name
    - } else {
        a(href="/auth/")=i18n.get('auth.login.login')
    - }
